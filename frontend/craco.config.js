module.exports = {
    devServer: {
        setupMiddlewares: (middlewares, devServer) => {
            // Add any custom middleware or configuration if necessary
            return middlewares;
        },
    },
};
